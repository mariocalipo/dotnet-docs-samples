name: Build and Push Docker Image - dev

on:
  pull_request:
    types:
      - closed
    branches:
      - dev

jobs:
  build_dev:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Build and Publish Solution
        run: |
          cd netcore
          dotnet publish -c Release

      - name: Build Docker Image - dev
        run: |
          echo "Building Docker image for dev environment"
          
          # Generate a unique tag using the pull request number and GitHub SHA
          PR_NUMBER=$(echo "${{ github.event.pull_request.number }}")
          TAG="dev-pr-${PR_NUMBER}-${GITHUB_SHA}"

          # Navigate to the netcore folder
          cd netcore

          # Build the Docker image with the generated tag
          docker build -t dotnetapp:$TAG .
  
  push_dev:
    runs-on: ubuntu-latest

    needs: build_dev

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Push Docker Image - dev
        run: |
          echo "Pushing Docker image for dev environment"
          # docker login -u $ARTIFACTORY_USERNAME -p $ARTIFACTORY_PASSWORD your-artifactory-registry
          # docker push your-artifactory-registry/your-docker-image:dev-$GITHUB_SHA
