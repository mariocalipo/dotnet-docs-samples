apiVersion: v1
kind: Service
metadata:
  name: prod-app-service
  labels:
    app: prod-app
spec:
  selector:
    app: prod-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer
