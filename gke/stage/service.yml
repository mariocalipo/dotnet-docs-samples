apiVersion: v1
kind: Service
metadata:
  name: stage-app-service
  labels:
    app: stage-app
spec:
  selector:
    app: stage-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer
