apiVersion: v1
kind: Service
metadata:
  name: ${ENVIRONMENT}app
spec:
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: ${ENVIRONMENT}app
  type: NodePort