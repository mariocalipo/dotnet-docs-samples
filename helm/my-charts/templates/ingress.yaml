apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.environment }}app
spec:
  defaultBackend:
    service:
      name: {{ .Values.environment }}app
      port:
        number: 80